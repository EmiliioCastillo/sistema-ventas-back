package com.projectjava.demosclient.mapper;

/*
import com.projectjava.demosclient.dto.ClienteDTO;
import com.projectjava.demosclient.entity.Cliente;

public class ClienteMapper {
    // Convertir cliente JPA Entity into UserDto
    public static ClienteDTO mapToClienteDto(Cliente cliente){
        ClienteDTO clienteDto = new ClienteDTO(
                cliente.getNombre(),
                cliente.getApellido(),
                cliente.getDescripcion(),

        );
        return clienteDto;
    }


    // Convertir clienteDTo al cliente JPA Entity
    public static Cliente mapToCliente(ClienteDTO clienteDto){
        Cliente cliente = new Cliente(
                clienteDto.getNoBanco(),
                clienteDto.getNombreBanco(),
                clienteDto.getEstatus()
        );
        return cliente;
    }

}
*/